# Documentation index

Godharness's own standard schema and default suite content are open decisions (see the
repository README's "Open Decisions" section); this index stays small until they're settled.

- [Local development](local-development.md) — the build and the checks CI runs.

Product scope, architecture, and the resolver design will each get their own document here
once those decisions are made, rather than being drafted speculatively now.
